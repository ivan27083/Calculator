#include <iostream>
#include "functions_calc.h"
#include <cmath>
using namespace std;

/*int main(int argc, char** argv) {
	double* tochki;
	tochki = (double*)malloc(sizeof(double));
	int count = 0;
	for (int i = 0; i < 5; i++) {
			tochki = (double*)realloc(tochki, (count + 1) * sizeof(double));
			tochki[count] = i;
			count++;
		}
	for (int j = 0; j < count; j++) {
		cout << "x" << j + 1 << "= " << tochki[j] << endl;
	}
}*/